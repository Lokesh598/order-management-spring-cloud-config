#this is repitative configation property in all microservice.
spring:
  application:
    name: config-dev
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8767/eureka  #eureka endpoint
